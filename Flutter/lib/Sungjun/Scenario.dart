import 'package:flutter/material.dart';

class ScenarioPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Scenario Page'),
      ),
      body: Center(
        child: Text('This is the Scenario Page'),
      ),
    );
  }
}
